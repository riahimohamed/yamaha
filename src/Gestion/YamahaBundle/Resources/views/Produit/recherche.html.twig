<form action="{{ path('recherche_produit') }}" method="post" class="navbar-form navbar-left" role="search">
    <div class="form-group">
        {{ form_widget(form.recherche, { 'attr': {'class': 'form-control', 'placeholder': 'Recherche par matricule'} }) }}
    </div>
    <button type="submit" class="btn"><span class="glyphicon glyphicon-search"></span></button>
</form>